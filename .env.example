# Neon Database Connection String
DATABASE_URL=your-neon-database-url-here

# NextAuth Configuration
# In development, NEXTAUTH_URL is automatically detected
# In production, set it to your domain (e.g., https://yourdomain.com)
# NEXTAUTH_URL=http://localhost:3000

# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here

# Email Capture API Configuration
# Generate API key with: openssl rand -hex 32
EMAIL_CAPTURE_API_KEY=your-api-key-here
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Shopify Integration
# Get from Shopify Admin > Settings > Apps > Develop apps
SHOPIFY_ADMIN_ACCESS_TOKEN=shpat_your_admin_access_token_here
SHOPIFY_SHOP_DOMAIN=yourstore.myshopify.com